<%= f.label "current_pick_#{team.parameterize.underscore}".to_sym do %>
  <%= f.radio_button :current_pick, team, id: "current_pick_#{team.parameterize.underscore}", checked: current_pick == team, disabled: current_pick.present? && current_pick != team %>
  <%= team %>
<% end %>
